# 设备联调与排障细节

本文记录已经通过实际设备确认的连接信息、串口映射和排障结论。涉及 SSH、串口、HC-14、飞控通信或地面站/机载上位机操作异常时，先阅读本文，再修改配置、接线或代码。

本文不保存 SSH 密码。凭据以 `AGENTS.md` 或当前用户明确提供的信息为准，不要把密码写入脚本、日志或命令输出。

## 1. 已确认设备

### 地面站树莓派

- SSH 地址：`192.168.31.107`
- SSH 用户：`cooper`
- 主机名：`pi`
- 无线网卡：`wlan0`，已确认地址为 `192.168.31.107/24`
- HC-14：`/dev/ttyUSB0`
- HC-14 USB 标识：CH340，USB ID `1a86:7523`
- RC927/STM32 屏幕串口候选：`/dev/ttyACM0`，USB ID `0483:5740`
- 板载串口链接：`/dev/serial0 -> ttyS0`

### 机载上位机

- SSH 地址：`192.168.31.176`
- SSH 用户：`fc`
- SSH 别名：`fc`
- 主机名：`fc-ubuntu`
- 无线网卡：`wlp1s0`，已确认地址为 `192.168.31.176/24`
- HC-14：`/dev/ttyUSB0`
- HC-14 USB 标识：CH340，USB ID `1a86:7523`

不要在 HC-14 透传测试中打开地面站的 `/dev/ttyACM0`。现有地面站工具将其作为 RC927/STM32 屏幕或触摸输入串口候选；正式程序接入前仍需确认屏幕协议。机载端的飞控串口才是 `/dev/ttyACM0`，两台主机上的同名设备不能混用。

## 2. HC-14 已确认参数

两端模块通过只读 AT 查询确认参数一致：

```text
串口格式：9600 8N1
无线信道：C28
空中速率：S3
发射功率：+20 dBm
```

只读查询及响应：

```text
AT     -> OK
AT+B?  -> OK+B:9600
AT+C?  -> OK+C:28
AT+RX  -> OK+B:9600 / OK+C:28 / OK+S:3 / OK+P:+20dBm
```

未经当前任务明确要求，不要发送修改信道、波特率、速率、功率或恢复出厂设置的 AT 指令。

## 3. 串口控制线要求

两端通过 CH340 打开 HC-14 串口时，必须明确关闭 DTR 和 RTS：

```python
ser = serial.Serial()
ser.port = "/dev/ttyUSB0"
ser.baudrate = 9600
ser.rts = False
ser.dtr = False
ser.open()
ser.setRTS(False)
ser.setDTR(False)
```

不要直接依赖 `serial.Serial("/dev/ttyUSB0", 9600)` 的默认控制线状态。本次排障中，默认状态使模块响应 `ORDER ERROR`，并能响应 AT 查询，表现为进入配置模式；按已有工具明确设置 `DTR=False`、`RTS=False` 后恢复正常双向透传。

看到连续的 `ORDER ERROR\r\n` 时，优先检查：

1. 打开串口的程序是否明确关闭 DTR 和 RTS；
2. HC-14 的 KEY 是否被控制线或外部接线拉入配置模式；
3. 两端是否都使用 `9600 8N1`；
4. 是否有其他进程占用 `/dev/ttyUSB0`。

不要把 `ORDER ERROR` 首先归因于无线丢包，也不要在未查询当前配置前恢复出厂设置。

## 4. 已有联调工具

双 SSH 主机联调工具：

```text
C:\Users\TZDEZACR\Desktop\ground_station\tools\hc14_remote_link_test.py
```

该文件位于另一个工程，仅作为人工联调工具参考，不要在本仓库中增加对它的运行时依赖。工具默认配置为：

```text
地面站：cooper@192.168.31.107:/dev/ttyUSB0
机载端：fc@192.168.31.176:/dev/ttyUSB0
波特率：9600
DTR：False
RTS：False
```

2026-07-10 实际联调结果：

```text
地面站 -> 机载端：10/10，丢包 0
机载端 -> 地面站：10/10，丢包 0
RESULT: BIDIRECTIONAL LINK CONFIRMED
```

该结果只确认当时近距离、每秒一包条件下的双向通信，不代表远距离、飞行中干扰、高吞吐或长期稳定性已经验证。

## 5. 推荐排障顺序

1. 分别确认 `192.168.31.107:22` 和 `192.168.31.176:22` 可达并能 SSH 登录。
2. 在两端执行只读设备检查，确认 HC-14 仍映射为 `/dev/ttyUSB0`，并优先核对 `/dev/serial/by-id/`。
3. 检查是否有进程占用 `/dev/ttyUSB0`，不要强制终止未知任务进程。
4. 使用 `9600 8N1` 且明确设置 `DTR=False`、`RTS=False`。
5. 先执行少量带唯一编号的单向消息，再执行反向消息，分别统计期望数量、实收数量和缺失编号。
6. 只有出现 `ORDER ERROR` 或无法透传时，才进行只读 AT 查询；查询后重新按关闭 DTR/RTS 的方式打开串口。
7. 需要修改模块参数、接线或飞控代码时，先停止并重新确认任务授权和安全边界。

## 6. 安全边界

- HC-14 联调只使用两端 `/dev/ttyUSB0`。
- 不运行解锁、起飞、模式切换、速度控制、导航或执行器程序。
- 不因 HC-14 故障修改 `python_sdk/FlightController/**`，除非已经沿调用链确认问题位于底层且用户明确要求修复。
- 测试完成后关闭两端串口和 SSH 会话。
- 无法完成真实距离、运动状态或复杂电磁环境测试时，必须明确标记为未验证。
