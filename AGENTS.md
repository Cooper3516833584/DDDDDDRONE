# AGENTS.md

本文件用于约束代码代理在 `DDDDDDRONE` 仓库中的阅读、修改、验证与结果汇报行为。

本仓库包含真实无人机的飞控通信、导航、雷达、视觉、串口屏、电机与任务流程代码。任何修改都应默认按“会连接真实硬件并影响飞行安全”处理，而不能按普通 Python 示例项目处理。

地面站主控树莓派192.168.31.107，用户cooper密码11223344
无人机上位机ssh配置如下
Host fc
  HostName 192.168.31.176
  Port 22
  User fc
password:11223344

## 故障排查资料

仓库根目录的 `details.md` 记录已确认的设备地址、串口映射、通信参数、联调工具和历史排障结论。

当 SSH、串口、HC-14、飞控通信或地面站/机载上位机操作出现失败、异常输出，或行为与历史结果不一致时，必须先完整阅读 `details.md`，再决定是否修改配置、接线或代码。`details.md` 仅作为操作与排障参考，不得覆盖本文件中的飞行安全和底层保护规则。

## 代码修改与部署唯一通道

机载上位机和地面站上的仓库仅作为部署副本，不得作为开发工作区。所有受 Git 跟踪内容（包括代码、配置、脚本、测试、文档和 `AGENTS.md`）必须先在本地开发机完成修改与验证，再提交并推送到对应的 GitHub 仓库，最后通过 SSH 在目标设备的仓库中拉取。

严格禁止通过 SSH 或任何基于 SSH 的工具在机载上位机或地面站直接创建、修改、删除、移动或覆盖仓库文件，包括但不限于：

- 远程编辑器、终端编辑器和 IDE Remote SSH；
- `sed -i`、`tee`、重定向、here-document、脚本写文件或 Python 临时改写；
- `scp`、`sftp`、`rsync` 或管道传输文件到设备；
- 在设备仓库中执行 `git commit`、`git merge`、`git rebase`、`git cherry-pick` 或创建开发分支；
- 以紧急修复、临时调试或部署方便为由直接改动设备上的代码或配置。

SSH 对设备仓库的允许操作仅限于：

1. 使用 `git status`、`git diff`、`git log`、`git rev-parse` 等命令进行只读核验；
2. 确认本地改动已经推送到正确的 GitHub 仓库和目标分支；
3. 确认设备仓库工作树干净、当前分支正确且能够快进；
4. 执行 `git pull --ff-only`；
5. 拉取后再次核验提交号和工作树状态。

若设备仓库存在未提交改动、未推送提交、分支分叉、冲突或无法快进，必须立即停止部署并向用户报告。禁止在设备上使用 `git stash`、`git reset`、`git checkout`、`git restore`、`git clean`、强制拉取或手工合并来处理；应在本地开发机和 GitHub 中查明并解决后，再让设备通过 `git pull --ff-only` 更新。

通过 SSH 拉取仅表示同步已经审核并推送到 GitHub 的代码，不授权启动飞行任务、访问硬件、修改系统配置或执行本文件禁止的任何程序。

---

## 1. 总体目标

代码代理修改本仓库时，优先保证：

1. **飞行与硬件安全不被削弱**
2. **现有飞控通信协议与控制接口保持兼容**
3. **只做完成当前任务所需的最小修改**
4. **任务层修改不得无故扩散到底层控制层**
5. **不擅自运行会驱动无人机、雷达、相机、电机、PWM、蜂鸣器或串口屏的程序**
6. **不因整理代码而改变坐标系、单位、方向、时序、线程或状态机语义**
7. **无法完成真实硬件验证时，必须如实说明，禁止假称已验证**

稳定性和可控性高于代码形式上的整洁。

---

## 2. 适用范围与指令优先级

本文件默认适用于整个仓库。

当子目录中存在更具体的 `AGENTS.md` 时：

- 距离目标文件更近的规则优先；
- 但不得降低本文件规定的飞行安全、底层保护和结果告知要求；
- 用户在当前任务中的明确要求优先于本文件。

除非任务明确涉及 STM32 固件，代码代理默认工作范围是：

```text
python_sdk/
```

仓库根目录下的 MCU 驱动、飞控固件和板级代码属于更高风险区域。未被任务明确点名时，不得修改。

---

## 3. 项目结构与修改等级

### 3.1 受保护的底层控制层

以下目录整体属于受保护区域：

```text
python_sdk/FlightController/
```

其中包括但不限于：

```text
python_sdk/FlightController/Base.py
python_sdk/FlightController/Serial.py
python_sdk/FlightController/Protocal.py
python_sdk/FlightController/Application.py
python_sdk/FlightController/Remote.py
python_sdk/FlightController/__init__.py
python_sdk/FlightController/Components/
python_sdk/FlightController/Solutions/
```

这些代码承担或直接参与：

- 飞控串口收发；
- 数据帧解析与校验；
- ACK、重试、心跳和断线处理；
- 飞行模式、解锁、起飞、降落和急停相关控制；
- 实时速度和姿态控制；
- 雷达数据解析；
- 导航、定位、轨迹规划和传感器融合；
- 与串口屏、RealSense、ROS 等硬件或系统组件通信。

**默认不得修改 `python_sdk/FlightController/**`。**

只有同时满足以下条件时，才允许修改：

1. 已确认问题确实位于底层，而不是任务脚本的调用方式错误；
2. 无法通过任务层的小范围兼容处理解决；
3. 修改范围可以控制在与问题直接相关的局部；
4. 不改变无关协议、接口、时序和默认行为；
5. 最终结果中明确告知用户修改了底层控制代码。

若必须修改，必须遵守：

- 只做局部补丁，禁止重写整文件；
- 不顺手重构邻近代码；
- 不批量整理 import、注释、类型标注或格式；
- 不修改公开方法名、参数顺序、返回结构和异常语义，除非任务明确要求；
- 不改变协议命令字、数据帧格式、大小端、校验、ACK 或重试规则，除非任务本身就是修复这些内容；
- 不扩大修改到多个底层文件，除非存在直接依赖且必须同步；
- 在结果中写明修改原因、精确文件、影响接口、验证方式和未完成的硬件验证。

### 3.2 任务层与当前开发代码

以下内容通常属于任务层，可根据需求进行修改：

```text
python_sdk/*.py
python_sdk/SolutionsNew/
python_sdk/testcode/
```

典型内容包括：

- 比赛或任务流程；
- 目标点和任务参数；
- 串口屏交互流程；
- 视觉任务编排；
- 雷达导航任务编排；
- 电机、PWM、蜂鸣器等任务动作；
- 临时调试和验证脚本。

当前顶层候选入口和辅助文件包括但不限于：

```text
python_sdk/2022_24.py
python_sdk/2022_24_noscreen.py
python_sdk/2022_24_noscreen_nomotor.py
python_sdk/xunxian.py
python_sdk/screen_manager.py
python_sdk/test_*.py
```

这些文件是否仍在使用，必须根据当前任务、启动命令和调用关系判断，不能只根据文件名判断。`screen_manager.py` 与任务脚本中可能存在重叠的屏幕管理逻辑，禁止在未确认实际入口时静默迁移、合并或删除其中一份实现。

任务层允许修改不代表可以随意重构。仍然必须遵守最小修改原则和硬件安全规则。

仓库中可能同时存在名称相近的多个任务脚本，例如带有 `noscreen`、`nomotor` 等后缀的版本。代理必须先确认当前任务实际针对哪个入口文件，禁止默认把一个版本的改动同步到所有相似文件。

除非用户明确要求，禁止：

- 将多个任务脚本强行合并；
- 抽象出大规模新框架；
- 为了“消除重复”而改写现有稳定任务流程；
- 同时修改所有历史版本或备用版本；
- 将任务参数、坐标、阈值和 PID 参数统一成代理自行判断的数值。

### 3.3 历史代码目录

```text
python_sdk/former_code/
```

该目录用于存放已经停用、被替代或仅供参考的历史任务代码。

默认规则：

- 历史代码视为只读参考；
- 不在当前功能中新增对 `former_code` 的运行时依赖；
- 不主动修复其中的旧问题；
- 不因为文件“看起来暂时没用”就擅自移动；
- 不把底层控制库、共享模型、公共资源或当前入口放入该目录。

只有在用户明确要求，或已能确认文件被新实现完全替代且没有当前入口、import、脚本或文档引用时，才可将代码移入 `former_code`。

移动时应：

1. 优先使用保留历史的文件移动方式；
2. 搜索文件名、模块名和入口命令的引用；
3. 更新仍然有效的 import、启动说明或脚本引用；
4. 保持归档文件原内容，避免在移动时顺便重写；
5. 不要求所有历史文件仍能在当前环境独立运行；
6. 在结果中列出移动的文件及判定其已停用的依据。

---

## 4. 硬性修改规则

### 4.1 最小修改原则

- 只修改完成当前任务必需的文件和代码。
- 不顺手修复无关问题。
- 不进行全仓库格式化、导入排序、批量重命名或目录重组。
- 能通过修改一个调用点解决的问题，不要改底层公共接口。
- 能通过增加小型保护条件解决的问题，不要重写状态机。
- 能通过复用现有函数解决的问题，不要创建平行实现。
- 不因为代理认为代码“写得不好”就替换现有实现。

如果实际 diff 明显大于需求本身，应先缩小方案，而不是继续扩散修改范围。

### 4.2 兼容性规则

除非用户明确要求破坏性变更，必须保持：

- 现有模块导入路径；
- 现有类名和函数名；
- 现有公开参数和默认值；
- 现有命令字和配置键；
- 现有任务启动方式；
- 现有串口屏命令格式；
- 现有日志目录和主要日志语义；
- 现有返回值、事件和回调结构；
- 现有数据单位与坐标方向。

仓库中存在一些已经被其他代码依赖的历史拼写，例如文件名或接口名中的非标准拼写。除非任务明确要求迁移，禁止仅为纠正拼写而重命名，例如：

```text
Protocal.py
stablize
```

如确实需要改名，必须提供兼容层并更新所有调用点，不能直接删除旧名称。

### 4.3 编码和换行

- 文本文件使用 UTF-8。
- 优先保持目标文件现有换行风格。
- 不因编辑器保存导致整文件换行或编码变化。
- 含中文注释、中文提示或串口屏文案的文件不得整文件重写。
- 不批量润色中文文案。
- 不将正常中文替换为转义序列或乱码形式。

### 4.4 依赖规则

- 非必要不得新增第三方依赖。
- 优先使用标准库和仓库已有依赖。
- 必须新增依赖时，应更新对应平台的依赖文件并说明原因。
- 不得把仅适用于 x86/N97 的依赖无条件加入 ARM64/STM32MP257 环境。
- 不得把仅适用于 ARM64 的系统包假设写入通用 Python 依赖。
- 可选硬件依赖应尽量保持可选导入或明确降级路径，不能让无该硬件的环境在导入阶段直接崩溃。
- 不擅自升级 OpenCV、NumPy、SciPy、ONNX Runtime、RealSense、ROS 等关键依赖版本。
- 不替换模型文件、权重文件或二进制资源，除非任务明确要求。

---

## 5. 飞行安全规则

### 5.1 禁止自动执行真实硬件入口

在没有用户明确授权且无法确认硬件环境安全时，不得运行任何可能执行以下行为的脚本：

- 连接 `/dev/ttyACM*`、`/dev/ttyUSB*` 或真实串口；
- 解锁无人机；
- 起飞、移动、改变高度、返航或降落；
- 启动实时速度控制；
- 驱动步进电机、PWM、蜂鸣器或其他执行器；
- 打开真实相机、雷达或 RealSense 并进入持续采集；
- 向串口屏持续发送命令；
- 启动完整任务主程序。

包括名称中含有 `test`、`noscreen` 或 `nomotor` 的脚本，也不能据此假定安全。脚本即使不控制某一种外设，也可能仍会解锁或驱动无人机。

默认只做静态检查和不触发硬件的纯逻辑验证。

### 5.2 不得削弱的保护逻辑

未经用户明确要求，不得删除、绕过或放宽：

- 飞行模式检查；
- 解锁状态检查；
- 连接状态检查；
- 遥测有效性检查；
- 高度和定位有效性检查；
- 超时处理；
- 停止事件；
- 零速度或悬停兜底；
- 降落和锁桨流程；
- 急停入口；
- 雷达帧头、长度和 CRC 校验；
- 串口数据帧校验；
- ACK、重试、发送锁和心跳逻辑。

遇到异常路径时，优先保持或恢复：

```text
停止水平运动 -> 稳定/悬停 -> 降落 -> 必要时锁桨
```

具体顺序应服从现有接口和任务状态，不得机械插入可能导致更危险行为的命令。

### 5.3 模式、时序与实时控制

不得随意修改：

- HOLD_ALT、HOLD_POS、PROGRAM 等模式编号和含义；
- 解锁、起飞、切换模式、开始导航之间的时序；
- 实时控制帧发送频率；
- 控制帧失效时间；
- 心跳和断线重连间隔；
- ACK 等待和重试次数；
- 任务停止等待时间；
- 电机和 PWM 动作持续时间。

实时控制通常依赖持续刷新。禁止将持续发送逻辑改成只发送一次，也不得在导航暂停或传感器失效时删除零速度兜底。

### 5.4 单位与坐标系

本项目不同模块可能使用不同单位和方向约定。修改前必须沿调用链确认，禁止凭名称猜测。

重点检查：

- 位置是米还是厘米；
- 速度是米每秒还是厘米每秒；
- 高度来源是融合高度、气压高度、ToF 或其他传感器；
- 角度是度还是弧度；
- 偏航正方向是顺时针还是逆时针；
- 图像坐标、雷达坐标、机体坐标和地图坐标的轴方向；
- 导航层和路径规划层之间是否存在米与厘米的转换；
- 协议层是否对某个轴或偏航符号做了反向处理。

禁止为了“看起来更直观”而删除符号变换或单位换算。

若任务确实要求改变坐标、单位或方向，必须：

1. 检查所有生产者、消费者和显示层；
2. 明确写出旧约定和新约定；
3. 更新直接相关注释；
4. 增加纯逻辑验证；
5. 在结果中标记为高风险变更。

---

## 6. 通信与协议层规则

### 6.1 飞控串口协议

修改 `Base.py`、`Serial.py`、`Protocal.py` 或其调用者时，必须保护：

- 帧头和帧尾；
- 命令字、选项值和模式值；
- 字段长度和字段顺序；
- 大小端；
- 校验算法；
- ACK 匹配；
- 超时和重试；
- 发送锁；
- 接收线程和缓冲区行为；
- 心跳和自动重连；
- 回调调用顺序；
- 原始遥测到状态对象的单位转换。

禁止在没有协议文档或上下位机对应代码佐证时，猜测某个保留字段“无用”并删除。

### 6.2 雷达数据

修改雷达代码时，必须保留或等价实现：

- 波特率与设备识别逻辑；
- 固定帧长度检查；
- 帧头检查；
- CRC 校验；
- 不完整帧和错位帧处理；
- 数据解析后的角度、距离和置信度含义；
- 雷达线程停止和资源释放。

不得为了提高“容错率”而直接接受未通过校验的数据。

### 6.3 串口屏

修改串口屏相关代码时，必须确认：

- 文本编码仍为 UTF-8；
- 命令结束符仍符合屏幕协议；
- 接收数据类型和值的分隔规则没有被破坏；
- 队列、事件和超时语义保持一致；
- 未知命令不会触发飞行任务；
- 重复命令不会意外启动多个任务线程；
- 急停命令始终具有高优先级且可重复调用。

不要在未确认屏幕固件同步更新的情况下修改命令字符串。

### 6.4 FC_Server / FC_Client 架构

上位机上通常运行着 `server_ros.py`，它实例化 `FC_Server` 并持有飞控串口，同时在 TCP 5654 端口（authkey `b"fc"`）接受客户端连接。`FC_Client` 通过网络连接到 `FC_Server`，将 `send_data_to_fc` 调用转发到服务器执行，自身不打开任何串口。

编写需要连接飞控的代码（测试脚本、调试工具等）时，**必须先询问用户：上位机上是否正在运行 `server_ros.py`（或其他启动了 `FC_Server` 的程序）。**

- **如果正在运行**：使用 `FC_Client`，通过 `connect()` 方法连接到服务器，避免抢占飞控串口。参考 `testcode/test_indicator_led.py`。
- **如果未运行**：使用 `FC_Controller`，通过 `start_listen_serial()` 直连飞控串口。参考 `read_fc_mode.py`。

不得在未确认上位机状态的情况下默认选择直连串口，以免与已运行的 `FC_Server` 抢占飞控连接。

### 6.5 飞行代码的 ROS 建图组件启动

如果已经确认上位机开机自启动 `python_sdk/server_ros.py`，后续编写或修改会在上位机运行并使用 `navi` 导航的飞行代码时，不得把 `server_ros.py` 正在运行等同于 ROS 建图链路已经启动。此类飞行代码默认必须确保以下组件已经启动并可用：

- RealSense ROS 驱动；
- 雷达 ROS 驱动；
- Cartographer；
- `camera_pose_frame` 与 `base_link` 所需的静态 TF。

飞行代码默认直接沿用 `python_sdk/base_test.py` 中通过 `RosManager` 启动上述组件的现有方式，不得为此另行设计或修改新的启动、复用、进程探测或健康检查框架。进入 `navi` 导航、解锁或起飞前，必须等待 RealSense 位姿、雷达数据、地图和导航所需 TF 均已建立且保持新鲜；等待超时或任一链路无效时必须拒绝起飞，不得仅凭 `FC_Client` 已连接或 `server_ros.py` 正在运行继续任务。

---

## 7. 线程、事件与资源管理

本项目大量使用线程、事件、队列和回调。除非任务明确要求，禁止将现有并发模型整体改写为 `asyncio`、多进程或其他框架。

修改时必须注意：

- 线程是否为 daemon；
- 线程退出条件是否可达；
- `Event.set()`、`clear()` 和 `wait()` 的顺序；
- 停止任务后是否仍可能发送控制命令；
- 同一任务是否可能被重复启动；
- 回调是否在串口接收线程内执行；
- 回调中是否加入了长耗时工作；
- 是否存在无休眠的忙循环；
- 共享状态是否需要现有锁保护；
- 关闭时是否释放串口、相机、雷达和网络资源；
- `join()` 是否设置合理超时，避免急停流程永久阻塞。

禁止：

- 用强制结束线程代替正常停止事件；
- 删除现有发送锁；
- 在串口回调中直接执行完整任务；
- 因异常而静默继续发送旧控制量；
- 吞掉所有异常后继续飞行；
- 让急停等待一个无法结束的工作线程。

对任务停止和急停相关修改，应检查重复调用是否安全，以及每个循环是否定期检查停止事件。

---

## 8. 导航、视觉与任务流程规则

### 8.1 导航

修改导航和轨迹逻辑时：

- 不随意改变 PID 参数、限幅、到点阈值或超时；
- 不默认改变定位来源和融合模式；
- 不删除定位无效时的停止输出；
- 不改变目标点坐标单位；
- 不把路径规划内部单位直接当作飞控控制单位；
- 不改变到点判定方式，除非需求明确且有验证；
- 不在导航线程中加入无界阻塞操作；
- 不忽略 `no_path`、超时或定位丢失等失败状态。

若任务需要修改绕障、巡路或轨迹生成，应优先在对应任务或算法模块中做局部修改，不要改飞控协议层。

### 8.2 视觉

修改视觉代码时：

- 保持模型输入尺寸、颜色空间、归一化和输出解析一致；
- 不擅自替换模型文件；
- 不默认改变摄像头编号、分辨率或帧率；
- 不删除目标丢失时的停止或悬停行为；
- 不把耗时推理放入飞控串口回调；
- 保持角度方向转换与飞控接口约定一致；
- 不将调试窗口、图片保存或高频日志默认打开在正式任务中。

### 8.3 任务脚本

修改任务脚本前，必须先读完整的任务入口、停止路径和资源初始化部分，至少确认：

- 飞控对象在哪里创建；
- 串口、相机、雷达和屏幕在哪里初始化；
- 任务线程如何启动；
- 急停如何触发；
- 任务如何结束；
- 降落和锁桨在哪里执行；
- 目标点和高度参数来自哪里；
- 同名辅助函数是否在其他文件中有不同版本。

不要只修改正常流程而忽略异常、取消和急停流程。

---

## 9. 配置、路径、日志与生成文件

### 9.1 路径

- 不新增仅在代理本机有效的绝对路径。
- 保持现有设备路径可配置或与当前平台约定一致。
- 访问模型和资源时优先基于脚本位置构造路径，而不是依赖随机当前工作目录；但不得在无需求时大规模改写现有路径逻辑。
- 修改启动目录相关行为时，应检查相对 import、模型路径、日志路径和图片输出路径。

### 9.2 日志

- 保持现有 Loguru 或项目日志风格。
- 不在高频控制循环中增加逐帧 INFO 日志。
- 不默认记录完整图像、点云、串口原始流或大块二进制数据。
- 不把敏感网络信息、用户数据或无必要的绝对路径写入日志。
- 调试日志应可关闭，不能永久影响实时性能。

### 9.3 运行时文件

除非任务明确要求，禁止提交：

```text
__pycache__/
*.pyc
python_sdk/fc_log/
临时截图
雷达调试图片
相机采集图片
临时模型输出
本地 IDE 状态文件
```

验证过程中产生的未跟踪临时文件应避免提交；若同类缓存、日志或调试产物反复出现，应优先更新 `.gitignore`。

若相关文件已经被 Git 跟踪，不要为保持工作树干净而擅自回退、删除或重置；应向用户说明并等待授权，或在提交时排除。

不得将已有运行时产物当作源码进行批量格式化或迁移。

---

## 10. Python 实现要求

- 优先保持与仓库现有 Python 版本兼容；默认不要使用仅在 Python 3.10 及以上可用的新语法。
- 保持现有模块导入风格，避免无关的相对/绝对 import 改写。
- 类型标注应服务于本次修改，不做全文件补标。
- 不为小修引入新架构、依赖注入框架或配置系统。
- 不用宽泛的 `except Exception: pass` 隐藏控制错误。
- 捕获异常时，应确保飞行控制进入已有安全路径，或将异常继续上抛给能处理它的层级。
- 不在循环中新增无界增长的列表、队列或缓存。
- 不在实时线程中进行不必要的磁盘 I/O、图片编码或大数组复制。
- 新增循环必须具备退出条件，并在适当位置让出 CPU。
- 对硬件句柄使用与现有代码一致的关闭和清理方式。

---

## 11. 修改前工作流程

开始修改前必须：

1. 阅读目标文件完整上下文，而不是只看搜索命中的几行；
2. 阅读目标函数的直接调用者和被调用者；
3. 确认当前实际使用的任务入口；
4. 搜索是否存在同名函数、复制版本或历史实现；
5. 判断修改是否会触及 `FlightController`；
6. 识别涉及的硬件、线程、单位、坐标系和安全状态；
7. 优先设计任务层的最小补丁；
8. 明确哪些验证可以在无硬件环境完成。

若发现需求必须大范围改动，应先尽量拆成小的兼容步骤，不能直接重构整个任务系统。

---

## 12. 验证要求

本项目不要求使用 `pytest`，也不得为了满足形式要求而新建 pytest 测试框架。

### 12.1 默认静态验证

对所有修改，按适用性执行：

```bash
python3 -m py_compile <修改的 Python 文件>
```

多个文件可逐个检查，或只对受影响的小目录执行：

```bash
python3 -m compileall -q <受影响的小目录>
```

同时检查：

```bash
git diff --check
git status --short
git diff --stat
git diff -- <相关文件>
```

确认：

- 没有语法错误；
- 没有多余空白和冲突标记；
- 本次提交或交付不包含无关文件；
- 没有因换行或编码造成整文件 diff；
- 本次提交或交付不包含生成文件、运行时产物或临时文件；
- `FlightController` 是否被意外改动。

如发现新的缓存、日志、调试图片、临时输出或本地 IDE 文件类型，优先补充 `.gitignore`；若这些文件已经被 Git 跟踪，应说明 `.gitignore` 不会忽略已跟踪文件，是否执行 `git rm --cached` 由用户决定。

### 12.2 纯逻辑验证

对于不访问真实硬件的纯逻辑，可使用小型一次性脚本验证，例如：

- 数据帧打包与解析；
- CRC 或校验算法；
- 坐标变换；
- 单位换算；
- 路径规划纯函数；
- 到点判定；
- 命令解析；
- 目标去重和状态转换。

验证脚本应避免导入后自动初始化串口、相机、雷达或飞控的模块。临时验证文件不要提交到仓库。

### 12.3 导入验证

只有在确认模块导入不会启动硬件、创建长期线程或执行任务时，才可做 import 验证。

某些模块在导入时可能配置日志、创建目录或依赖可选硬件库。缺少依赖时，应说明环境限制，不得为了让导入通过而随意删除可选功能。

### 12.4 硬件验证

除非用户明确要求并确认安全环境，否则不执行真实硬件验证。

无法执行硬件验证时，应在结果中明确列出需要人工验证的项目，例如：

- 飞控串口能否连接；
- 遥测是否正常；
- 急停是否可用；
- 实时控制是否持续刷新；
- 雷达帧是否稳定；
- 屏幕命令是否正确；
- 电机/PWM 方向和持续时间是否正确；
- 起飞、悬停、导航、返航和降落流程是否安全。

禁止把“通过语法检查”描述成“飞行验证通过”。

---

## 13. 修改 `FlightController` 时的额外流程

当且仅当本次任务确实需要修改 `python_sdk/FlightController/**` 时，必须额外完成：

1. 写明为什么不能在任务层解决；
2. 确认修改是否影响协议、单位、线程或飞行状态；
3. 搜索该接口的所有调用点；
4. 将改动限制在最小函数；
5. 为纯逻辑部分增加可复现的最小验证；
6. 检查正常路径、超时路径、断线路径和停止路径；
7. 在最终结果中单独增加“底层控制代码修改”说明。

该说明至少包含：

- 修改文件；
- 修改函数或类；
- 修改原因；
- 行为变化；
- 明确未变化的协议或接口；
- 已执行验证；
- 未执行的真实硬件验证；
- 可能风险和回退方式。

即使修改只有一行，也不能省略告知。

---

## 14. 结果输出要求

完成任务后，结果必须包含以下内容：

### 修改摘要

说明实际改变了什么行为，不要只写“优化了代码”。

### 修改文件

列出所有修改、移动、新增或删除的文件。

### 底层控制代码状态

必须明确写出以下两种情况之一：

```text
未修改 python_sdk/FlightController/**
```

或：

```text
已修改 python_sdk/FlightController/**，具体为……
```

若已修改，必须按上一节详细说明。

### 验证

列出实际执行的命令和结果。

### 未验证内容

说明因缺少硬件、依赖、模型、摄像头、雷达、飞控或系统环境而无法验证的内容。

### 风险与边界

指出仍可能存在的风险、依赖的前提和未覆盖的异常路径。

禁止：

- 声称未实际运行的测试已通过；
- 用“应该没问题”替代验证结果；
- 隐瞒底层控制代码改动；
- 省略真实硬件未验证说明。

---

## 15. 明确禁止的行为

代码代理不得：

- 为小需求整文件重写；
- 默认修改 `python_sdk/FlightController/**`；
- 未告知用户就修改底层控制代码；
- 批量格式化整个仓库；
- 批量修改换行符或中文文案；
- 擅自统一多个任务脚本；
- 擅自把当前代码移动到 `former_code`；
- 从 `former_code` 新增正式运行时依赖；
- 删除急停、超时、悬停、降落或锁桨保护；
- 改变飞行模式编号或协议命令字；
- 猜测并更改坐标方向、角度正负和单位；
- 在未授权情况下运行真实飞行或执行器脚本；
- 用真实硬件试飞代替静态审查；
- 吞掉异常后继续使用旧控制量；
- 引入无关依赖或升级关键依赖；
- 发现日志、缓存、调试图片或临时文件反复出现时，仍不更新 `.gitignore`；
- 仅为修正拼写破坏已有接口；
- 在未验证的情况下宣称问题已完全修复。

---

## 16. 推荐工作顺序

每次任务按以下顺序执行：

1. 确认实际任务入口和目标文件；
2. 阅读相关上下文与调用链；
3. 标记是否涉及真实硬件和 `FlightController`；
4. 设计任务层最小补丁；
5. 只修改必要位置；
6. 执行不触发硬件的静态或纯逻辑验证；
7. 检查 diff、编码、换行和生成文件；
8. 再次确认底层目录是否被修改；
9. 按规定格式汇报修改、验证、风险和未验证内容。

若 diff 超出预期，应优先回退并缩小补丁，而不是继续扩大重构。

---

## 17. 本项目默认优先级

发生冲突时，默认按以下优先级决策：

1. **人员与飞行安全**
2. **急停、降落和控制兜底可用性**
3. **飞控协议与底层接口兼容性**
4. **当前任务正确性**
5. **现有任务脚本兼容性**
6. **最小修改和可回退性**
7. **性能与可维护性**
8. **代码风格和形式整洁**

任何“更优雅”的方案，只要会扩大飞行风险、底层改动范围或回归范围，都不应作为默认方案。
